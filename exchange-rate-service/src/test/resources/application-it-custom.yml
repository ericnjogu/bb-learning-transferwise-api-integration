wise:
  api-key: test-api-token
  base-url: https4://localhost:${wiremock.server.https-port}
  endpoints:
    exchange-rate-get:
      httpMethod: GET
      url: ${wise.base-url}/v1/rates

eureka:
  client:
    enabled: false